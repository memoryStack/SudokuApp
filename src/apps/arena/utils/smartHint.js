import { generateNewSudokuPuzzle } from '../../../utils/util'

// deep clone the mainNumbers
// write this in JS and if performance is not good then shift to native side
const getSmartHint = async ({ row, col }, originalMainNumbers) => {
    // generateNewSudokuPuzzle
    // make a copy of mainNumbers and generate notes of all kinds
    return null
}

export { getSmartHint }
