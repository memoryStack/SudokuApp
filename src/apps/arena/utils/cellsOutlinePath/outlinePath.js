
// TODO: let's do it later, after the UI testing
const getCellsOutlinePath = (cells) => {

    const disjointCellSets = getDisjointSets(cells)

}

export const getDisjointSets = (cells) => {
    return cells



}
