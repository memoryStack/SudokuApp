import { INITIAL_STATE } from '../apps/arena/store/state/smartHintHC.state'

export type RootState = {
    smartHintHC: typeof INITIAL_STATE,
        // refree: refreeReducers,
        // gameState: gameStateReducers,
        // boardController: boardControllerReducers,
        // board: boardReducers,
}
